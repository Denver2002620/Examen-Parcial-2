#include<iostream>
#include "ejercicio3.h"
int main()
    {
  for (int i=0;i<8;i++)//bucle for para poner las notas una por una
    {
    int n;//declarar la variable de nota
    int s=0;//declarar la variable en el que vamos a sumar
    std::cout<<"ingressa la nota"<<i+1<<":";//pedir de ingressar la nota
    std::cin>>n;//leer la nota
    s=s+n;//sumar la nota con ellas mismas
    if (i==7)
      {
      v=s/8;//calculamos la media
      }

    }
    }
