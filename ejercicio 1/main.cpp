#include<iostream>
#include "main.h"
int main ()
{
    int a;//declarar variable que el utilisador va a ingressar
    std::cout<<"ingressa un numero intero porfavor";
    std::cin>>a;//leer el numero que el utilizador a ingressado
    verif(a);// hemos llamado la funcion verif para verificar si es positivo o negativo o 0
}
